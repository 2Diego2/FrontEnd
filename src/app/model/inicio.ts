export class Inicio {
    idInicio: number;
    logo: string;
    linkPagina: string;
  
    constructor(idInicio: number, logo: string, linkPagina: string) {
      this.idInicio = idInicio;
      this.logo = logo;
      this.linkPagina = linkPagina;
    }
  }
  